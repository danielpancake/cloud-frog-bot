version: '3.8'
name: cloudfrogbot

services:
  cache:
    image: redis:latest
    restart: always
    container_name: r1
    ports:
      - "6379:6379"
    volumes: 
      - cache:/data
volumes:
  cache:
    driver: local
